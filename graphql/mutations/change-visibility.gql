# https://staging.cms.bbnavi.de/visibility/PointOfInterest/523913/false

mutation (
  $id: ID!,
  $recordType: String!,
  $visible: Boolean!
) {
  changeVisibility (
    id: $id,
    recordType: $recordType,
    visible: $visible
  ) {
    id
    status
    statusCode
  }
}
