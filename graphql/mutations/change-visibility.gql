mutation (
  $id: ID!,
  $recordType: String!,
  $visible: Boolean!
) {
  changeVisibility (
    id: $id,
    recordType: $recordType,
    visible: $visible
  ) {
    id
    status
    statusCode
  }
}
