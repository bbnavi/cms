mutation (
  $forceCreate: Boolean,
  $id: ID,
  $name: String!
  $description: String!
) {
  createPointOfInterest (
    forceCreate: $forceCreate,
    id: $id,
    name: $name,
    description: $description
  ) {
    id
    name
  }
}
